import { LightningElement } from 'lwc';

export default class ApiPractise extends LightningElement {

    parentData = 'Parent Data'
    userDetails = {
        firstname: 'ABC',
        lastname: 'xyz'
    };
}